# Copy this to .env and fill in real values. Never commit .env itself.

# Same Postgres instance/DB as backend — copy the value from backend/.env
DATABASE_URL="postgresql://postgres:phoenix@123@localhost:5433/real_estate_os?schema=public"


GROQ_API_KEY=your-groq-api-key-here

PORT=8000
CORS_ORIGIN=http://localhost:3000